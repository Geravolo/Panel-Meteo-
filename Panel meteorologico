<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Panel Meteorológico GV</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background: linear-gradient(to bottom, #0f2027, #203a43, #2c5364);
      color: #fff;
      margin: 0;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    h1 {
      font-size: 2rem;
      margin-bottom: 10px;
    }
    .panel {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      width: 100%;
      max-width: 1000px;
    }
    .card {
      background-color: #1e2a38;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    }
    .card h2 {
      font-size: 1.2rem;
      margin-top: 0;
    }
    .data {
      font-size: 1rem;
      margin-top: 10px;
      white-space: pre-line;
    }
    footer {
      margin-top: 30px;
      font-size: 0.9rem;
      color: #ccc;
    }
  </style>
</head>
<body>
  <h1>Panel Meteorológico GV</h1>
  <div class="panel">
    <div class="card">
      <h2>METAR - SUMU</h2>
      <div class="data" id="metar-sumu">Cargando...</div>
    </div>
    <div class="card">
      <h2>TAF - SUMU</h2>
      <div class="data" id="taf-sumu">Cargando...</div>
    </div>
    <div class="card">
      <h2>METAR - SAEZ</h2>
      <div class="data" id="metar-saez">Cargando...</div>
    </div>
    <div class="card">
      <h2>TAF - SAEZ</h2>
      <div class="data" id="taf-saez">Cargando...</div>
    </div>
  </div>
  <footer>Actualización automática cada hora. Fuente: DINACIA y NOAA</footer>
  <script>
    const urls = {
      'metar-sumu': 'https://aviacioncivil.mil.uy/opmet/data/metar/SUMU.xml',
      'taf-sumu': 'https://aviacioncivil.mil.uy/opmet/data/taf/SUMU.xml',
      'metar-saez': 'https://aviacioncivil.mil.uy/opmet/data/metar/SAEZ.xml',
      'taf-saez': 'https://aviacioncivil.mil.uy/opmet/data/taf/SAEZ.xml'
    };

    const proxy = 'https://api.allorigins.win/raw?url=';

    async function fetchAndDisplay(id, url) {
      try {
        const response = await fetch(proxy + encodeURIComponent(url));
        const text = await response.text();
        const parser = new DOMParser();
        const xml = parser.parseFromString(text, 'application/xml');
        const metData = xml.querySelector('data').textContent.trim();
        document.getElementById(id).textContent = metData;
      } catch (error) {
        document.getElementById(id).textContent = 'Error al cargar';
      }
    }

    function updateAll() {
      for (const id in urls) {
        fetchAndDisplay(id, urls[id]);
      }
    }

    updateAll();
    setInterval(updateAll, 3600000); // 1 hora
  </script>
</body>
</html>
